# secret_cleaner
A "Privacy-First Gateway" that sits between the professional and the AI. It automatically redacts PII (Personally Identifiable Information) before sending data to the API and ensures no data is used for model training.
